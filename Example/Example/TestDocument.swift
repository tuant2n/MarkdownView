//
//  TestDocument.swift
//  Example
//
//  Created by 秋星桥 on 6/29/25.
//

import Foundation

let testDocument = ###"""
我来为你的正则表达式模式添加一些能够被捕获的测试内容。以下是修改后的示例，包含了各种数学公式格式：

## 修改后的示例内容

好的，我们来解这道解析几何题。

**解：**

**(1) 求 C 的离心率**

首先，我们需要建立椭圆的标准方程。对于一般的椭圆，我们有：

$$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1$$

```math
$$\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1$$
```

其中 $a > b > 0$。

1. 已知椭圆 C 的方程为 \\(\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1\\) (其中 \\(a > b > 0\\))。

2. 点 A(0, 3) 在椭圆上，将其坐标代入椭圆方程，得到：
   \\[\frac{0^2}{a^2} + \frac{3^2}{b^2} = 1 \implies \frac{9}{b^2} = 1\\]
   解得 \\(b^2 = 9\\)，因为 \\(b > 0\\)，所以 \\(b = 3\\)。

3. 点 \\(P(3, \frac{3}{2})\\) 也在椭圆上，将其坐标和 \\(b^2 = 9\\) 代入椭圆方程：

   $$\frac{3^2}{a^2} + \frac{(\frac{3}{2})^2}{9} = 1$$

   化简得：
   \\[
   \frac{9}{a^2} + \frac{\frac{9}{4}}{9} = 1 \\
   \frac{9}{a^2} + \frac{1}{4} = 1 \\
   \frac{9}{a^2} = \frac{3}{4}
   \\]

   解得 \\(a^2 = 12\\)。

4. 根据椭圆的性质，我们知道：
   $$c^2 = a^2 - b^2$$
   因此：\\(c^2 = 12 - 9 = 3 \implies c = \sqrt{3}\\)。

5. 离心率的公式为：
   \\[e = \frac{c}{a} = \frac{\sqrt{3}}{\sqrt{12}} = \frac{\sqrt{3}}{2\sqrt{3}} = \frac{1}{2}\\]

所以，椭圆 C 的离心率为 **\\(\frac{1}{2}\\)**。

---

**(2) 求直线 *l* 的方程**

对于三角形面积的计算，我们使用坐标公式：

$$S = \frac{1}{2} |x_1(y_2 - y_3) + x_2(y_3 - y_1) + x_3(y_1 - y_2)|$$

设点 B 的坐标为 \\((x_B, y_B)\\)，则面积约束条件为：

\\[
\begin{align}
S &= \frac{1}{2} |0(y_B - \frac{3}{2}) + 3(3 - y_B) + x_B(\frac{3}{2} - 3)| \\
9 &= \frac{1}{2} |9 - 3y_B - \frac{3}{2}x_B| \\
18 &= |9 - 3y_B - \frac{3}{2}x_B|
\end{align}
\\]

这给出两种情况的约束方程。

对于二次方程的求解，我们得到：
$$7y_B^2 + 18y_B - 9 = 0$$

使用求根公式：
\\[y_B = \frac{-18 \pm \sqrt{18^2 - 4(7)(-9)}}{2(7)} = \frac{-18 \pm \sqrt{576}}{14} = \frac{-18 \pm 24}{14}\\]

综上所述，直线 *l* 的方程为 **\\(3x - 2y - 6 = 0\\)** 或 **\\(5x - 46y + 54 = 0\\)**。

## 新增的数学内容特点

这个修改后的示例包含了所有你的正则表达式模式能够捕获的内容：

1. **`$$...$$`** - 块级公式
2. **`\\[...\\]`** - 带转义的块级公式  
3. **`\\(...\\)`** - 带转义的行内公式
4. **多行块级公式** - 包含换行的复杂公式结构

这样你就可以用这个示例来测试你的正则表达式模式是否能正确捕获各种LaTeX数学公式格式了！
"""###
